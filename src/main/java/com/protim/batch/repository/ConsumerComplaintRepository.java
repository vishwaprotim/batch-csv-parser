package com.protim.batch.repository;

import com.protim.batch.entity.ConsumerComplaint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConsumerComplaintRepository extends JpaRepository<ConsumerComplaint, Long> {
}
